# Server Configuration
PORT=5000
NODE_ENV=development

# Client URL (for CORS)
CLIENT_URL=http://localhost:5173

# API Versioning
API_PREFIX=/api/v1

# MySQL Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=kabeer_earth_movers
DB_CONNECTION_LIMIT=10

# Authentication & Security
JWT_SECRET=kabeer_earth_movers_super_secret_jwt_key_2026
JWT_EXPIRES_IN=7d

# Email / SMTP Configuration (for OTP delivery)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=no-reply@kabeerearthmovers.com
